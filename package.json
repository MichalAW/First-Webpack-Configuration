{
    "name": "20.3.First-Webpack-Configuration",
    "version": "1.0.0",
    "description": "The first working configuration on the repository",
    "main": "script.js",
    "scripts": {
        "test": "npm-run-all",
        "test:echo": "echo \"Error: no test specified\" && exit 1",
        "start:project": "npm install && npm-run-all init:*",
        "init:dirs": "mkdirp sass css",
        "init:files": "touch README.md index.html sass/style.scss ",
        "init:gitignore": "curl https://raw.githubusercontent.com/github/gitignore/master/Node.gitignore -o .gitignore",
        "build": "npm-run-all build:* test",
        "build:clean": "rimraf css/style.css",
        "build:sass": "node-sass --output-style compact -o css sass",
        "build:autoprefixer": "autoprefixer-cli css/style.css",
        "build-dev:sass": "node-sass --output-style expanded --source-map true -o css sass",
        "watch": "npm-run-all build -p watch:*",
        "watch:sassprefixer": "node-sass --output-style expanded --source-map true -o css sass --watch && onchange -d 100 css/style.css -- autoprefixer-cli -o css/style.prefixed.css css/style.css",
        "watch:browsersync": "browser-sync start --server --files css/*.css *.html"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/MichalAW/20.3.First-Webpack-Configuration.git"
    },
    "keywords": [],
    "author": "MichalAW michal.wanta@gmail.com",
    "license": "ISC",
    "bugs": {
        "url": "https://github.com/MichalAW/20.3.First-Webpack-Configuration/issues"
    },
    "homepage": "https://github.com/MichalAW/20.3.First-Webpack-Configuration#readme",
    "devDependencies": {
        "autoprefixer-cli": "^1.0.0",
        "browser-sync": "^2.26.3",
        "node-sass": "^4.11.0",
        "onchange": "^5.2.0"
    },
    "dependencies": {
        "npm-run-all": "^4.1.5",
        "sass": "^1.16.0",
        "save-dev": "^2.0.0"
    }
}
